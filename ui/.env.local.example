# ============================================================
# RAG UI — Environment Variables
# Copy to .env.local and fill in your values.
# ============================================================

# URL of the Python FastAPI backend (used server-side in API routes)
BACKEND_URL=http://localhost:8000

# URL exposed to the browser for direct fetches (optional, falls back to localhost)
NEXT_PUBLIC_API_URL=http://localhost:8000
